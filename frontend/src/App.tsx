export function App(): JSX.Element {
  return (
    <main className="app-shell">
      <section className="hero">
        <p className="eyebrow">anki-vocab-bot</p>
        <h1>Admin UI scaffold is ready.</h1>
        <p className="lede">
          Phase 1 sets up the React, TypeScript, and Vitest foundation. Feature pages land in later
          phases.
        </p>
      </section>
    </main>
  );
}
